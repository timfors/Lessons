{>"layout" /}

{<body}
	<h1> {#currentLessons} {day} {/currentLessons} </h1>
	<scan id="hred"> 
		<a href="/{day - 1}">Previous</a>
		<a href="/{day}">Today</a>
		<a href="/{day + 1}">Next</a>
	</scan>
	<ol class="lessons"> 
		{#currentLessons}
			{#lessons}
				<li class="lessons"> {.} </li>
			{/lessons}
		{/currentLessons}
		<p class="smile"> {face} </p>
	</ol>
{/body}